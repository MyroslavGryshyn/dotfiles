[env]
  # TERM variable
  #
  # This value is used to set the `$TERM` environment variable for
  # each instance of Alacritty. If it   
  # is not present, alacritty will
  # check the local terminfo database and use `alacritty` if it is available,
  # otherwise `xterm-256color` is used.
  TERM = "xterm-256color"
  live_config_reload = "true"

[window]
  # Window dimensions (changes require restart)
  #
  # Specified in number of columns/lines, not pixels.
  # If both are `0`, this setting is ignored.   
  opacity = 0.8
  blur = true
  dimensions = { columns = 0, lines = 0 }

  # Removes top bar
  decorations = "full"

  # Window padding (changes require restart)
  #
  # Blank space added around the window in pixels. This padding is scaled
  # by DPI and the specified value is always added at both opposing sides.
  padding = { x = 2, y = 2 }

  # Spread additional padding evenly around the terminal content.
  dynamic_padding = false

  # When   
  # true, alacritty starts maximized.
  startup_mode = "Maximized"

[scrolling]
  # Maximum number of lines in the scrollback buffer.
  # Specifying '0' will disable scrolling.
  history = 10000

  # Number of lines the viewport will move for every line scrolled when
  # scrollback is enabled (history > 0).
  multiplier = 3

  # Faux Scrolling
  #
  # The `faux_multiplier` setting controls the number of lines the terminal
  # should scroll when the alternate screen buffer is active. This is used
  # to allow mouse scrolling for applications like `man`.   

[font]
size = 16.0

[font.normal]
family = "JetBrainsMono Nerd Font"
# family = "Menlo"

# The `style` can be specified to pick a specific face.
style = "Regular"

  # Bold font face
[font.bold]
# If the bold family is not specified, it will fall back to the
# value specified for the normal font.
family = "JetBrainsMono Nerd Font"

# The `style` can be specified to pick a specific face.   


  # Italic font face
  # italic = {
  #   # Font family
  #   #
  #   # If the italic family is not specified, it will fall back to the
  #   # value specified for the normal font.
  #   family = "monospace"
  #
  #   # The `style` can be specified to pick a specific face.
  #   style = "Italic"   

  # }

  # Offset is the extra space around each character. `offset.y` can be thought of
  # as modifying the line spacing, and `offset.x`   

  # Thin stroke font rendering (macOS only)
  # AppleFontSmoothing = true

[colors.primary]
foreground = "#e0def4"
background = "#232136"
dim_foreground = "#908caa"
bright_foreground = "#e0def4"

[colors.cursor]
text = "#e0def4"
cursor = "#56526e"

[colors.vi_mode_cursor]
text = "#e0def4"
cursor = "#56526e"

[colors.search.matches]
foreground = "#908caa"
background = "#393552"

[colors.search.focused_match]
foreground = "#232136"
background = "#ea9a97"

[colors.hints.start]
foreground = "#908caa"
background = "#2a273f"

[colors.hints.end]
foreground = "#6e6a86"
background = "#2a273f"

[colors.line_indicator]
foreground = "None"
background = "None"

[colors.footer_bar]
foreground = "#e0def4"
background = "#2a273f"

[colors.selection]
text = "#e0def4"
background = "#44415a"

[colors.normal]
black = "#393552"
red = "#eb6f92"
green = "#3e8fb0"
yellow = "#f6c177"
blue = "#9ccfd8"
magenta = "#c4a7e7"
cyan = "#ea9a97"
white = "#e0def4"

[colors.bright]
black = "#6e6a86"
red = "#eb6f92"
green = "#3e8fb0"
yellow = "#f6c177"
blue = "#9ccfd8"
magenta = "#c4a7e7"
cyan = "#ea9a97"
white = "#e0def4"

[colors.dim]
black = "#6e6a86"
red = "#eb6f92"
green = "#3e8fb0"
yellow = "#f6c177"
blue = "#9ccfd8"
magenta = "#c4a7e7"
cyan = "#ea9a97"
white = "#e0def4"
