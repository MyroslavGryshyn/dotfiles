#Use up 2 or up 3 instead of ../..
upup(){ DEEP=$1; [ -z "${DEEP}" ] && { DEEP=1; }; for i in $(seq 1 ${DEEP}); do cd ../; done; }
alias up='upup'

# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'

    alias grep='grep --color=auto'
    alias egrep='egrep --color=auto'
fi

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# git aliases
alias gs='git status'
alias gd='git diff'
alias gc='git commit -am'
alias gch='git checkout'
alias glist='git stash list'

alias gentags='ctags -R .'

alias hg='history | grep'
alias pg='ps aux | grep'
alias fgrep='fd'

alias pipr='pip install -r requirements.txt'

alias ipy="python -c 'import IPython; IPython.terminal.ipapp.launch_new_instance()'"

alias open_changed="vim -p \$(git status --porcelain | awk '{print $2}')"

alias gl="git log --oneline --graph"

# Crypto arbitrage bot
_CRYPTO_DIR="/Users/myroslavhryshyn/Dev/crypto"
_CRYPTO_PID="$_CRYPTO_DIR/logs/bot.pid"
alias run_crypto_prod='[ -f $_CRYPTO_PID ] && kill -0 $(cat $_CRYPTO_PID) 2>/dev/null && echo "Bot already running (PID: $(cat $_CRYPTO_PID))" || { cd $_CRYPTO_DIR && nohup .venv/bin/python3.14 main.py > /dev/null 2>&1 & echo "Bot started (PID: $!)"; }'
alias log_crypto_prod='tail -f $_CRYPTO_DIR/logs/bot.log'
alias kill_crypto_bot='[ -f $_CRYPTO_PID ] && kill $(cat $_CRYPTO_PID) && echo "Bot stopped" || echo "No bot process found"'
